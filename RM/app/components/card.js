import { View, Text, Image, StyleSheet } from "react-native";
import React from "react";



const Card = ({ characters }) => {
/*     let display;
    if(characters) {
        display = results.map(x => {
            let { id, name, image, status, species, type, gender, origin, location } = x;
            return(
            <View key={id}>
                <View>
                    <button></button>
                    <Text>{name}</Text>
                </View>
                <View>
                    <Image source={image}/>
                    <View>
                        <Text>Status: {status}</Text>
                        <Text>Species: {species}</Text>
                        <Text>Type: {type}</Text>
                        <Text>Gender: {gender}</Text>
                        <Text>Origin: {origin.name}</Text>
                        <Text>Last location: {location.name}</Text>
                    </View>
                </View>

            </View>
        )
        })
    } else {
        display= "The character isn't found"
    }   
    return (
        <>{display}</>
    ) */
};

export default Card;